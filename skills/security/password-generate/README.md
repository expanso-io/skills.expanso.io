# password-generate

Generate secure random passwords using UUID-based entropy.

## Usage

```bash
echo "" | expanso-edge run pipeline-cli.yaml
```

## Output

```json
{
  "password": "a1b2c3d4e5f67890",
  "length": 16,
  "entropy_bits": 64
}
```
