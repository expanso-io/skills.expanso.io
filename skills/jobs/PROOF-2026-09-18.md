# Job run record, 2026-09-18

This is the dated execution record for the jobs in `skills/jobs/`. Each
job's `skill.yaml` `proof` block points here. It is sanitized: it holds no
credentials, API keys, bootstrap tokens, workspace hostname or private
configuration.

## Authorization

The captain authorized the `expanso-test-suite` Expanso Cloud workspace for
these runs on 2026-09-18. The authorization is kept in private records.

## Setup

- `expanso-edge` v2.1.21.
- One operator-registered node, name `jfp-cloud-20260918t025928z`, id
  `a706de39-f697-4f3a-b728-687400804b5f`, selected by the label
  `jfp_proof`. It is not a hosted runner.
- The jobs' dependencies ran on the same host as the node: fixture servers,
  PostgreSQL 14.23, Ollama 0.33.2 with `nomic-embed-text`, Qdrant 1.19.1.
- Workspace baseline before the runs: 0 nodes, 0 jobs.

## Cloud runs

Every execution below ran on node `a706de39-...`, and every check passed.
Each line is a Cloud job id followed by its execution id.

### First-run acceptance, 9/9

- `j-79a82a7b-4535-41d1-8e27-e2c999c3fdde` ->
  `e-fadfe5fa-d3b9-46b9-9b4c-5618a909ff79`

Receipt: the downstream sink received 5/5 records carrying the run nonce.

### rss-feed-engine, 14/14

- `j-381d077f-0147-4e09-a2cc-945a12de980b` ->
  `e-be8c7c1d-6d9b-40d8-ba67-ddfa030d8af8`
- `j-5488d350-5f38-4a93-bb80-c0de863ae2f3` ->
  `e-51dcd027-0d5c-436b-a1e0-a20e369e1a5e`
- `j-eec0d96c-0440-4088-8687-e2fb8cbdf2f5` ->
  `e-43a6056d-4015-455e-83d1-483ddb1d201b` (live NASA feed, 10 items)
- `j-9aceba8e-4bc7-4c1f-9e5d-1dfceb823756` ->
  `e-95589aa2-9495-4c81-b143-ffb0b21316a6` (live BBC feed, 21 items)

### data-migration-engine, 7/7

- `j-e4596cc6-9726-4d9d-8259-d395944ac463` ->
  `e-c924caf8-9fb4-479e-870a-554dab9008e1`
- `j-3876780e-1a68-470d-ad96-0a21a1b945b4` ->
  `e-0b11c104-1889-4ed3-9314-740a7a6c4f3d`

Receipt: 994 target rows identical to an independent SQL transform, 6
rejects, and the second run was idempotent.

### notification-engine, 7/7 (local test receiver only)

- `j-bd2f9df3-7a9c-48fd-bfb9-63564dd46c5d` ->
  `e-b85a0646-215f-48f7-ad5f-54c3c600bb20`

Receipt: 3 of 8 events delivered, 2 x 503 retried.

### rag-embed-retrieve, 12/12

- ingest: `j-3810ef93-fadd-4f36-b87d-48b993b64111` ->
  `e-e9fa6a2b-9001-42ec-be10-a16a6924cac8`
- ingest re-run: `j-c004b034-b325-4b55-8c33-ddb89ccb546f` ->
  `e-c83ee01d-7a3d-4ead-934c-34c98b2bcaf0`
- query: `j-d7572ac1-b6a0-4e6b-9240-3eb573d2251d` ->
  `e-ab9f752b-45db-4fcf-bf05-7c0cfe9bb805`

Receipt: 16 points, cosine 1.000000 against a direct embedding, and 4/4
questions returned the right chunk first.

## Cleanup

Every task job was stopped and deleted and the node record was deleted,
leaving the workspace at 0 nodes and 0 jobs. Private credential copies were
removed.

## Local-only jobs

`webhook-fan-out`, `log-reduction`, `sensor-telemetry-mqtt` and
`ci-fixtures` ran on a local-mode node (`expanso-edge run --local`) on
2026-09-18. They were never run through Cloud.

## What the published specs are

The spec bodies published in `skills/jobs/` are the docs recipe versions
(expanso-io/docs.expanso.io commit f6eab2b, draft) whose structure matches
the runs above; only the values listed in each spec's header comment
differ. The `rss-feed-engine` fetch-error guard after its `http` processor
is newer than the Cloud runs and was validated on a local-mode node only.

## RSS fetch-error guard (local-mode only, 2026-09-18)

The `mapping` right after the `http` processor in `rss-feed-engine` stops
the job with the fetch error before XML parsing. It was run on a
local-mode node (expanso-edge v2.1.21) against a local fixture, never on
Expanso Cloud:

| Run | Feed response | Job state | Records | Where it failed |
|---|---|---|---|---|
| Guarded | HTTP 200 | completed | 4 | did not fail |
| Guarded | HTTP 401 | failed | 0 | the guard: "feed fetch failed: ... 401 Unauthorized" |
| Guarded | HTTP 503 | failed | 0 | the guard: "feed fetch failed: ... 503" |
| Unguarded control | HTTP 401 | failed | 0 | the `xml` step |

This test did not reproduce a silent `completed` outcome for RSS: without
the guard the job still failed, one step later. The guard makes the cause
explicit and matches the lesson in `docs/llms.txt`. The separate finding
that an HTTP error can end as a `completed` job with nothing written came
from a different job, whose mapping turned the error body into zero
records.

## Validation versus execution

`validation-report.json` stays validation-only. Its readiness vocabulary
is capped at `validated-not-executed`, and this record does not change it.
This record is what establishes execution.
