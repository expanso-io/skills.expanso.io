name: array-unique
version: 1.0.0
description: Remove duplicate values from arrays

inputs:
  - name: array
    type: array
    required: true
    description: Array with potential duplicates

outputs:
  - name: unique
    type: array
    description: Array with duplicates removed

backends:
  - name: local
    type: local
    description: Local processing (no API required)

author: expanso
license: MIT
